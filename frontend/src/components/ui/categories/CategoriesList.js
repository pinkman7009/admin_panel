import React from 'react'

const CategoriesList = ({categories}) => {
    return (
        <div>
            list of categories
            {/* {categories.map(item => {
                return (
                    <div>
                        {item.name}
                    </div>
                )
            })} */}
        </div>
    )
}

export default CategoriesList
